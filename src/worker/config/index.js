export const config = {
  name: 'progressive-scheduler-cache-name',
  options: {
    headers: {
      'Content-Type': 'text/html'
    },
  },
  staticFiles: [
    "/",
    "/AppShell.bundle.js",
  ],
};

export default config;
